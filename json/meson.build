# Auto-converted from Makefile.am (best-effort)

# Installed headers
install_headers(['json.h'])

libjson_lib = static_library('libjson', [],
  install: true,
  include_directories: global_includes,
  c_args: global_c_args
)
